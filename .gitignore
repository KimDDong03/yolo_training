__pycache__/
*.pyc
node_modules/
.venv/
venv/

# 빌드 산출물 — npm run build 로 다시 만든다
frontend/dist/
frontend/tsconfig.tsbuildinfo

# 런타임 데이터 — 앱이 실행하면서 만든다
storage/
app.db
app.db-wal
app.db-shm
**/labels.cache

# 반입 패키지 (약 3 GB) — scripts/build_bundle.ps1 이 만든다
bundle/wheels/
bundle/prereq/
bundle/frontend-dist/
bundle/requirements.lock.txt
# scripts/fetch_weights.py 가 내려받는다
bundle/weights/*.pt

# 로컬 확인용 예제 데이터
examples/

.vscode/
.idea/

.codex/

# 기획서 PDF 빌드 중간 산출물 — build-handout-pdf.py 가 다시 만든다
docs/clip-pipeline/_handout.html
